window.onload = function () {
  let spinner = document.getElementById('spinner-container')
  let style = window.getComputedStyle(spinner)
  var matrix = new WebKitCSSMatrix(style.webkitTransform);
  debugger

}
